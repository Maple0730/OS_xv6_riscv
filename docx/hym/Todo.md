## 接口、设备与软件交互 — 待办事项

> 负责人：侯奕明
> 更新日期：2026-06-09

---

### 第一阶段：代码精读 + 画图（第14-15周，当前 ~ 6月中旬）

- [ ] 画出 **ecall → syscall → sret 完整时序图**（标注特权级切换）
- [ ] 画出 **时钟中断 → devintr → clockintr → yield 的数据流图**
- [ ] 画出 **键盘 → UART中断 → uartintr → consoleintr → shell读 的数据流图**
- [ ] 画出 **shell 执行 `ls | wc` 的 fork/pipe/dup/close 调用序列**
- [ ] 逐文件精读并写注释：
  - [ ] `trap.c` — trap 总控中心
  - [ ] `syscall.c` — 系统调用分发
  - [ ] `console.c` — 控制台 I/O
  - [ ] `uart.c` — UART 驱动
- [ ] 验证所有 **21 个系统调用**可用
- [ ] 用 GDB 单步跟踪一次 `write(1, "hello", 5)` 的全链路

---

### 第二阶段：功能实现 + 测试（第15-16周，6月中下旬）

#### 新增系统调用

- [x] **新增 `sys_ps` 系统调用** ✅ 已完成
  - `kernel/syscall.h` — 分配系统调用号 22
  - `kernel/syscall.c` — 注册到 syscalls[] 表
  - `kernel/sysproc.c` — 实现 sys_ps()，复用 procdump()
  - `user/usys.pl` — 生成汇编 stub
  - `user/user.h` — 声明 int ps(void)
  - `user/ps.c` — 用户程序

#### 验证实验

- [ ] **实验1：验证时钟中断频率**
  - 写用户程序调用 `uptime()` 验证约 100Hz
- [ ] **实验2：验证键盘中断**
  - 测试普通字符、退格、Ctrl-U、Ctrl-D、Ctrl-P 等场景
- [ ] **实验3：验证缺页异常（lazy allocation）**
  - 用 `sbrklazy()` 申请大量内存，观察 `vmfault()` 补页过程
- [ ] **实验4：验证系统调用全链路**
  - GDB 断点在 `usertrap()`、`syscall()`、`prepare_return()`

#### 测试程序

- [ ] 写 `pipetest.c` 用户程序验证管道
- [ ] 写 `intrtest.c` 用户程序验证中断/输入阻塞唤醒

---

### 第三阶段：文档 + 展示（第16-17周，6月下旬 ~ 汇报）

- [ ] 完成 **"中断与异常处理"模块文档**（对应 A 题第 2 项）
  - [ ] ecall 系统调用全链路分析
  - [ ] 时钟中断、设备中断、缺页异常处理
  - [ ] PLIC + 设备驱动框架
- [ ] 完成 **"用户程序加载与执行"中我负责的部分**
  - [ ] 系统调用封装库分析
  - [ ] shell 与内核接口分析
- [ ] 准备答辩 **PPT**（系统调用/中断/设备部分）
- [ ] 准备现场 **demo**（演示系统调用、中断处理、shell 操作）

---

### 扩展想法（视时间决定）

- [ ] 新增更多系统调用（如 `sys_meminfo` 内存状态观测 — 需配合韩硕的内存管理模块）
- [ ] 完善 shell 功能（如支持更多内置命令）
- [ ] 写更全面的系统调用测试集

---

### 与组员的待协调事项

| 事项 | 需要配合 | 状态 |
|------|---------|:----:|
| `yield()` 触发调度路径 | 涂凡琛 — 确认调度器接口 | 待沟通 |
| `vmfault()` 缺页处理 | 韩硕 — 确认 lazy allocation 实现细节 | 待沟通 |
| `sys_ps` 需要读取进程信息 | 涂凡琛 — 确认 proc 结构体字段 | 待沟通 |
| 内存状态观测 | 韩硕 — 需要内存管理模块提供接口 | 待沟通 |
