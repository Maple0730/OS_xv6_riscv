# PPT 图表生成提示词文档

> **用途**：本文件提供详细的提示词，可直接复制到 GPT-4o / Claude / Gemini 等图像生成工具中使用
>
> **风格**：学术报告风（深蓝 #1E3A8A 主色、白色背景、灰色辅助）
>
> **要求**：所有图表必须包含中文文字标签，数据符合操作系统理论

---

## 图片 1：课程知识体系思维导图

**文件名建议**：`knowledge_map.png`

**提示词**：

```
请为我生成一张中文学术风格的思维导图，主题是"操作系统 - 进程管理与处理机调度课程知识体系"。

【要求】
- 风格：学术报告风，深蓝色主调（#1E3A8A），白色背景
- 中文字体，使用简体中文
- 树形结构展示5个核心板块及其子内容
- 节点之间用带箭头的连接线表示依赖关系
- 适当使用浅色背景框区分不同板块

【结构要求】
中心节点：进程管理与处理机调度

5个一级子板块（每个用浅色矩形框包裹）：
1. 进程管理基础
   - PCB进程控制块
   - 进程状态机
   - fork/exec/wait/waitpid

2. 同步互斥
   - 信号量P/V操作
   - 管程Monitor
   - 条件变量

3. 死锁处理专题
   - 死锁四条件
   - 预防策略
   - 银行家算法
   - 检测与恢复

4. 经典调度算法
   - RR时间片轮转
   - FCFS先来先服务
   - SJF短作业优先
   - MLFQ多级反馈队列

5. 实时调度扩展
   - Rate-Monotonic
   - Earliest Deadline First

【连接关系】
- 进程管理基础 → 同步互斥
- 同步互斥 → 死锁处理
- 进程管理基础 → 经典调度算法
- 经典调度算法 → 实时调度扩展
- 同步互斥与经典调度算法为平级

【输出规格】
- 高清PNG，1920x1080或更高
- 适合插入PPT
```

---

## 图片 2：工程闭环流程图

**文件名建议**：`engineering_loop.png`

**提示词**：

```
请为我生成一张中文学术风格的工程方法论流程图，主题是"理论→内核→测试→验证的工程闭环"。

【要求】
- 风格：学术报告风，深蓝色（#1E3A8A）边框，浅蓝色（#DBEAFE）填充
- 简体中文
- 4个核心阶段用圆角矩形表示
- 阶段之间用带箭头的连线表示流向
- 在流程下方添加简短的说明文字

【四个阶段】
1. 背景理论
   - 教科书公式
   - 算法描述
   - 理论分析

2. 内核实现
   - 数据结构设计
   - 系统调用实现
   - 内核代码编写

3. 用户测试
   - 测试程序编写
   - 边界条件覆盖
   - 对比实验设计

4. 实测验证
   - 日志记录
   - 性能分析
   - Bug修复迭代

【布局】
四个阶段横向排列，从左到右依次为：理论 → 内核 → 测试 → 验证
最后用一条弧形箭头从"验证"指回"理论"，表示迭代优化

【输出规格】
- 高清PNG，1920x600或更高
- 适合作为PPT第3页的背景图
```

---

## 图片 3：进程状态转换图

**文件名建议**：`process_states.png`

**提示词**：

```
请为我生成一张中文学术风格的进程状态转换示意图，主题是"xv6进程状态机"。

【要求】
- 风格：学术报告风，深蓝色（#1E3A8A）节点，浅蓝色（#DBEAFE）背景
- 简体中文
- 6个状态用椭圆形或圆角矩形表示
- 状态之间用带箭头的弧线表示转换
- 每条弧线上标注转换触发条件

【6个状态】
1. UNUSED（未使用）— 浅灰色
2. USED（已分配）— 浅蓝色
3. RUNNABLE（可运行）— 绿色
4. RUNNING（运行中）— 橙色
5. SLEEPING（睡眠中）— 黄色
6. ZOMBIE（僵尸）— 红色

【转换关系及触发条件】
- UNUSED → USED：allocproc()分配
- USED → RUNNABLE：首次调度
- RUNNABLE → RUNNING：schedule()选中
- RUNNING → RUNNABLE：时间片用完
- RUNNING → SLEEPING：sleep()等待资源
- RUNNING → ZOMBIE：exit()退出
- SLEEPING → RUNNABLE：wakeup()唤醒
- ZOMBIE → UNUSED：父进程wait()回收

【布局】
将6个状态分布在画布上，UNUSED在左下，USED在其上方，RUNNABLE和RUNNING居中，SLEEPING在右上，ZOMBIE在右下
箭头要有方向性，重要转换用粗箭头

【输出规格】
- 高清PNG，1600x1200
- 适合PPT第4页
```

---

## 图片 4：哲学家就餐死锁示意

**文件名建议**：`dining_philosophers.png`

**提示词**：

```
请为我生成一张中文学术风格的哲学家就餐问题示意图，主题是"5个哲学家的死锁场景"。

【要求】
- 风格：学术报告风，深蓝色（#1E3A8A）主体
- 简体中文
- 俯视角度的圆桌场景
- 清晰展示死锁形成的环路

【布局】
- 中央：一张圆桌
- 圆桌周围：5个哲学家（用圆形或人形图标表示）
- 5个哲学家之间：5把叉子（用小矩形或餐具图标表示）
- 每个哲学家左手边一把叉子，右手边一把叉子

【标注】
- 哲学家编号：P0, P1, P2, P3, P4（顺时针方向）
- 叉子编号：F0, F1, F2, F3, F4
- 用红色箭头显示5个哲学家全部持有左叉
- 用虚线箭头显示5个哲学家都在等待右叉
- 5个红色箭头形成闭环：P0→P1→P2→P3→P4→P0

【关键标注】
- 标题："死锁场景：循环等待"
- 图例：
  - 红色实心箭头 = 已持有
  - 红色虚线箭头 = 等待中
- 底部说明："4个必要条件同时满足：互斥、占有并等待、不可剥夺、循环等待"

【输出规格】
- 高清PNG，1600x1200
- 适合PPT第10页
```

---

## 图片 5：MLFQ 实测结果柱状图

**文件名建议**：`mlfq_results.png`

**提示词**：

```
请为我生成一张中文学术风格的柱状图，展示MLFQ（多级反馈队列）调度算法的实测结果。

【数据】（来自xv6实测日志）
X轴类别：7个作业
- SHORT作业0（PID=9）：1 tick
- SHORT作业1（PID=10）：3 ticks
- SHORT作业2（PID=11）：3 ticks
- LONG作业0（PID=12）：5 ticks
- LONG作业1（PID=13）：6 ticks
- MIXED作业0（PID=14）：12 ticks
- MIXED作业1（PID=15）：12 ticks

Y轴：完成时间（ticks），范围0-15

【样式要求】
- 风格：学术报告风
- SHORT作业柱形：绿色（#10B981）
- LONG作业柱形：蓝色（#3B82F6）
- MIXED作业柱形：橙色（F59E0B）
- 每根柱子上方标注具体数值
- 柱形有黑色边框
- 背景白色，网格浅灰色虚线

【图表元素】
- 标题："MLFQ调度算法实测结果"（顶部居中）
- 副标题："短作业优先响应 - SHORT < LONG < MIXED"
- X轴标签："作业类型"
- Y轴标签："完成时间（ticks）"
- 图例：右上角，标注SHORT/LONG/MIXED对应的颜色

【关键标注】
- 用虚线标注"短作业平均≈2.3 tick"水平线
- 用虚线标注"长作业平均=5.5 tick"水平线

【输出规格】
- 高清PNG，1600x1000
- 适合PPT第9页
```

---

## 图片 6：调度算法对比雷达图

**文件名建议**：`sched_radar.png`

**提示词**：

```
请为我生成一张中文学术风格的雷达图（蛛网图），对比RR、FCFS、MLFQ三种调度算法。

【评估维度（5个）】
1. 公平性
2. 短作业响应
3. 长作业吞吐
4. 实现复杂度（值越低表示越简单）
5. 抗饥饿能力

【数据】（评分1-5，5为最优）
RR算法：
- 公平性：5
- 短作业响应：3
- 长作业吞吐：3
- 实现复杂度：5（最简单）
- 抗饥饿能力：4

FCFS算法：
- 公平性：3
- 短作业响应：2
- 长作业吞吐：5
- 实现复杂度：5
- 抗饥饿能力：3

MLFQ算法：
- 公平性：4
- 短作业响应：5
- 长作业吞吐：4
- 实现复杂度：3
- 抗饥饿能力：5

【样式要求】
- 风格：学术报告风
- 5个维度均匀分布在五边形上
- 评分刻度：1-5，中心为1，外圈为5
- 三种算法用不同颜色填充并半透明：
  - RR：蓝色（#3B82F6）半透明
  - FCFS：绿色（#10B981）半透明
  - MLFQ：橙色（#F59E0B）半透明
- 三条折线分别用对应颜色的实线

【图表元素】
- 标题："三种调度算法多维度对比"
- 副标题："（评分越高越优）"
- 图例：右上角

【输出规格】
- 高清PNG，1400x1400（正方形）
- 适合PPT第9页
```

---

## 图片 7：MLFQ 队列状态变化图

**文件名建议**：`mlfq_queues.png`

**提示词**：

```
请为我生成一张中文学术风格的MLFQ队列状态变化示意图。

【布局要求】
纵向分为3个队列区域（从高到低）：
- Q0（最高优先级）：浅绿色背景，时间片5ms
- Q1（中优先级）：浅蓝色背景，时间片10ms
- Q2（最低优先级）：浅橙色背景，时间片20ms

横向是时间轴：0ms, 5ms, 10ms, 15ms, 20ms, 25ms, 30ms

【显示3类作业的轨迹】

1. 短作业（绿色实线）：
   - 0-8ms 一直在Q0
   - 8ms处完成（标注"完成"）

2. 长作业（蓝色实线）：
   - 0-10ms 在Q0
   - 10ms处降级到Q2
   - 10-30ms 一直在Q2
   - 标注"降级"

3. I/O作业（红色实线）：
   - 0-30ms 始终在Q0（频繁sleep/wakeup）

【图表元素】
- 标题："MLFQ多级反馈队列：进程队列迁移"
- X轴标签："时间（ms）"
- Y轴标签："队列级别"
- 图例：右上角
- 在10ms处画一条虚线，标注"降级事件"
- 底部说明："每100 ticks优先级提升，防止饥饿"

【输出规格】
- 高清PNG，1800x900
- 适合PPT第8页
```

---

## 图片 8：死锁四条件维恩图

**文件名建议**：`deadlock_conditions.png`

**提示词**：

```
请为我生成一张中文学术风格的死锁四条件示意图。

【要求】
- 风格：学术报告风
- 四个圆形（或椭圆形）相互重叠
- 中心重叠区域用红色突出，标注"死锁"
- 每个圆形用不同颜色边框

【四个条件及其颜色】
1. 互斥条件（Mutual Exclusion）— 粉红色边框（#EC4899）
2. 占有并等待（Hold and Wait）— 紫色边框（#A855F7）
3. 不可剥夺（No Preemption）— 蓝色边框（#3B82F6）
4. 循环等待（Circular Wait）— 青色边框（#06B6D4）

【布局】
- 四个圆形排列成正方形或菱形
- 互斥 在左上
- 占有并等待 在右上
- 不可剥夺 在左下
- 循环等待 在右下
- 四个圆形的中心交集区域填充实色红色（#EF4444）
- 中心标注"死锁（Deadlock）"

【每个圆内的文字】
- 互斥："资源一次只能被一个进程使用"
- 占有并等待："持有资源时等待其他资源"
- 不可剥夺："资源不能强制夺走"
- 循环等待："形成进程等待环路"

【底部说明】
"死锁 = 4个条件同时满足；预防 = 破坏其中任一条件"

【输出规格】
- 高清PNG，1400x1200
- 适合PPT第10页
```

---

## 图片 9：银行家算法流程图

**文件名建议**：`banker_flowchart.png`

**提示词**：

```
请为我生成一张中文学术风格的银行家算法安全性检查流程图。

【要求】
- 风格：学术报告风，深蓝色（#1E3A8A）边框，浅色填充
- 简体中文
- 6个主要步骤用圆角矩形表示
- 决策点用菱形
- 用带箭头的连线连接

【流程步骤】
1. [开始] 接收资源请求（橙色填充）
2. [步骤] 模拟分配（绿色填充）
3. [判断] Need ≤ Work？（蓝色菱形）
   - 是：进入安全性算法
   - 否：拒绝请求

4. [分支A：安全] Grant Request（绿色填充）
5. [分支B：拒绝] Reject Request（红色填充）

【文字标注】
- 顶部标题："银行家算法 - 安全性检查流程"
- 在"模拟分配"步骤下方添加小字说明：
  "Work = Available + 当前进程的Allocation"
- 在判断为"否"时添加说明：
  "存在进程无法完成 → 不安全"

【布局】
纵向流程，从上到下：
开始 → 模拟分配 → 安全性判断 → 两条分支

【输出规格】
- 高清PNG，1400x1600
- 适合PPT第12页
```

---

## 图片 10：优先级反转时间线

**文件名建议**：`priority_inversion.png`

**提示词**：

```
请为我生成一张中文学术风格的优先级反转问题时间线图。

【场景】（1997年火星探测器Pathfinder Bug）
- 低优先级进程L持有锁
- 中优先级进程M在busy-loop
- 高优先级进程H等待L的锁

【要求】
- 风格：学术报告风
- 横向时间轴，从左到右
- 3个进程用不同颜色的横向条带表示
- 关键事件用箭头和文字标注

【布局】
时间轴刻度：t=0, t=4, t=10, t=18, t=20

3个进程条带（垂直堆叠）：
1. 高优先级H（红色，#EF4444）：在最上方
   - 0-10：执行中
   - 10-18：等待L的锁（红色斜线阴影）
   - 18-20：获得锁，继续执行

2. 中优先级M（蓝色，#3B82F6）：在中间
   - 0-20：持续busy-loop（没有阻塞）

3. 低优先级L（绿色，#10B981）：在最下方
   - 0-4：获得锁
   - 4-10：被M抢占，无法运行（橙色斜线）
   - 10-18：恢复执行，持有锁
   - 18：释放锁

【关键标注】
- 在H条带10-18区间标注"被L阻塞"
- 用红色箭头指向L的"被抢占"区域，标注"M抢占L"
- 标题："优先级反转问题（无继承机制）"
- 副标题："H实际等待时间 = M的busy-loop + L的关键区"

【输出规格】
- 高清PNG，1800x900
- 适合PPT第14页上半部分
```

---

## 图片 11：优先级继承时间线

**文件名建议**：`priority_inheritance.png`

**提示词**：

```
请为我生成一张中文学术风格的优先级继承解决时间线图。

【场景】
与上一张图相同的3个进程，但启用了优先级继承机制。

【要求】
- 风格：学术报告风
- 横向时间轴，从左到右
- 3个进程用横向条带表示
- 突出"优先级继承"的关键事件

【布局】
时间轴刻度：t=0, t=4, t=10, t=18, t=20

3个进程条带：
1. 高优先级H（红色）：在最上方
   - 0-4：执行
   - 4-16：等待L的锁（斜线阴影）
   - 16-20：获得锁，执行完成
   - 相比上张图，等待时间从18减到16

2. 中优先级M（蓝色）：在中间
   - 0-4：尝试执行
   - 4-16：被L的提升优先级抢占，无法运行
   - 标注"M无法抢占L"
   - 16-20：恢复执行

3. 低优先级L（绿色）：在最下方
   - 0-4：获得锁，开始执行
   - 4-16：继承H的优先级（用红色边框强调！）
   - 持续运行关键区
   - 16：释放锁，优先级恢复

【关键标注】
- L的条带4-16区间用红色边框包围
- 标注"L继承H的优先级"（红色文字）
- 用绿色箭头指向M的"被阻塞"区域
- 标题："优先级继承解决方案"
- 副标题："L临时获得H的优先级 → M无法抢占 → L快速完成"

【底部对比说明】
"对比：无继承时H等待18个时间单位；有继承时H仅等待16个时间单位"

【输出规格】
- 高清PNG，1800x900
- 适合PPT第14页下半部分
```

---

## 图片 12：RM vs EDF 对比图

**文件名建议**：`rm_vs_edf.png`

**提示词**：

```
请为我生成一张中文学术风格的RM与EDF实时调度对比图。

【测试配置】
3个周期性任务：
- T1：周期=10，执行=2，利用率=0.20
- T2：周期=20，执行=4，利用率=0.20
- T3：周期=40，执行=8，利用率=0.20
- 总利用率 = 0.60

【布局】
左右两个并排的柱状图：

【左图：Rate-Monotonic (RM)】
- 标题："Rate-Monotonic (RM) - 静态优先级"
- Y轴：CPU利用率 (0-1)
- X轴：3个任务
- 柱形高度：均为0.20
- 红色虚线：水平线 y=0.779
- 标注："RM可调度上限 n(2^(1/n)-1) = 0.779 (n=3)"
- 颜色：绿色（#10B981）

【右图：Earliest Deadline First (EDF)】
- 标题："Earliest Deadline First (EDF) - 动态优先级"
- Y轴：CPU利用率 (0-1.2)
- X轴：3个任务
- 柱形高度：均为0.20
- 绿色虚线：水平线 y=1.0
- 标注："EDF可调度上限 = 1.0 (100%)"
- 颜色：橙色（#F59E0B）

【底部对比表】
| 特性 | RM | EDF |
| 优先级类型 | 静态 | 动态 |
| 可调度上限 | 69.3% | 100% |
| 实现复杂度 | 简单 | 中等 |
| 适用场景 | 周期任务 | 动态截止时间 |

【总标题】
"实时调度算法对比：总利用率0.60 < 0.779 → 可调度"

【输出规格】
- 高清PNG，1800x1200
- 适合PPT第15页
```

---

## 图片 13：系统调用扩展概览

**文件名建议**：`syscall_extension.png`

**提示词**：

```
请为我生成一张中文学术风格的系统调用扩展统计图。

【数据】
5大类别，每个类别的系统调用数量：
1. 进程管理（Process Management）：8个
2. 同步互斥（Synchronization）：10个
3. 调度算法（Scheduling）：8个
4. 实时调度（Real-Time）：4个
5. IPC通信（IPC）：6个
合计：36个新增（从原23号扩展到66号）

【图表样式】
横向条形图（5个水平条）

每个条形用不同颜色：
1. 进程管理：深蓝色（#1E3A8A）
2. 同步互斥：绿色（#10B981）
3. 调度算法：橙色（#F59E0B）
4. 实时调度：紫色（#A855F7）
5. IPC通信：粉红色（#EC4899）

【布局】
Y轴（从上到下）：
- IPC通信 (6)
- 实时调度 (4)
- 调度算法 (8)
- 同步互斥 (10)
- 进程管理 (8)

X轴：系统调用数量 (0-12)

每根条形末端标注具体数字

【标题】
- 主标题："xv6系统调用扩展概览"
- 副标题："从#23扩展到#66，共新增36+个系统调用"

【底部说明】
"覆盖：进程管理 / 同步互斥 / 调度算法 / 实时调度 / IPC通信"

【输出规格】
- 高清PNG，1600x1200
- 适合PPT第3页或第18页
```

---

## 图片 14：测试覆盖度图

**文件名建议**：`test_coverage.png`

**提示词**：

```
请为我生成一张中文学术风格的测试覆盖度统计图。

【数据】
4大测试类别：
1. 基础测试（Basic Tests）：6个
   - semtest1/2/3
   - waitpidtest
   - fcfstest
   - mlfqtest

2. 死锁测试（Deadlock Tests）：4个
   - dining（复现）
   - dining_safe1/2（预防）
   - bankertest（银行家）

3. 同步测试（Sync Tests）：3个
   - monitortest
   - pc_monitor
   - msgqtest

4. 高级测试（Advanced Tests）：4个
   - prioritytest
   - pathfinder
   - rmtest
   - edftest
合计：17个测试程序

【图表样式】
横向条形图（4个水平条）

每个条形用不同颜色：
1. 基础测试：蓝色（#3B82F6）
2. 死锁测试：红色（#EF4444）
3. 同步测试：绿色（#10B981）
4. 高级测试：紫色（#A855F7）

【布局】
Y轴（从上到下）：
- 高级测试 (4)
- 同步测试 (3)
- 死锁测试 (4)
- 基础测试 (6)

X轴：测试程序数量 (0-8)

每根条形末端标注具体数字

【标题】
- 主标题："xv6进程管理测试覆盖度"
- 副标题："17个独立测试程序覆盖所有模块"

【底部说明】
"每个测试都有实测日志记录（docx/tfc/log/）"

【输出规格】
- 高清PNG，1600x900
- 适合PPT第16页
```

---

## 图片 15：调度器架构图

**文件名建议**：`scheduler_architecture.png`

**提示词**：

```
请为我生成一张中文学术风格的调度器架构图。

【要求】
- 风格：学术报告风，深蓝色（#1E3A8A）
- 简体中文
- 分层架构展示调度器整体设计

【分层结构】（从上到下）

第1层：入口层
- scheduler() 主函数（深蓝色框）
- 根据current_scheduler变量选择算法

第2层：调度器层（5个并排的圆角矩形）
1. rr_scheduler() - 时间片轮转（蓝色）
2. fcfs_scheduler() - 先来先服务（绿色）
3. sjf_scheduler() - 短作业优先（紫色）
4. mlfq_scheduler() - 多级反馈（橙色）
5. prio_scheduler() - 优先级调度（青色）

第3层：基础机制层
- wakeup() / sleep() 等待唤醒机制
- waitbucket 等待桶优化
- spinlock 自旋锁保护

第4层：硬件层
- 时钟中断（timer interrupt）
- 上下文切换（swtch）

【连接关系】
- 入口层 → 调度器层（带箭头的虚线）
- 调度器层 → 基础机制层
- 基础机制层 → 硬件层

【右侧注释栏】
"调度算法运行时可切换（#34 syscall）"
"时间片可动态配置（#35-36 syscall）"

【标题】
"xv6调度器架构"

【输出规格】
- 高清PNG，1800x1200
- 适合PPT第7页
```

---

## 图片 16：银行家算法安全状态示意

**文件名建议**：`banker_safe_state.png`

**提示词**：

```
请为我生成一张中文学术风格的银行家算法安全状态示意图，展示教科书经典T0状态。

【数据】Silberschatz教材T0状态

资源：3类 (A, B, C)，可用 = (3, 3, 2)

5个进程的状态：

| 进程 | Allocation | Max | Need | Available |
| P0 | (0, 1, 0) | (7, 5, 3) | (7, 4, 3) | (3, 3, 2) |
| P1 | (2, 0, 0) | (3, 2, 2) | (1, 2, 2) | |
| P2 | (3, 0, 2) | (9, 0, 2) | (6, 0, 0) | |
| P3 | (2, 1, 1) | (2, 2, 2) | (0, 1, 1) | |
| P4 | (0, 0, 2) | (4, 3, 3) | (4, 3, 1) | |

安全序列：<P1, P3, P4, P2, P0>

【布局要求】
- 左侧：大表格展示上述数据
- 右侧：5个进程用方框表示，按安全序列顺序排列
- 用箭头连接形成安全序列链
- 每个进程框内显示其Allocation和Need

【颜色编码】
- 已分配资源（Allocation）：浅蓝色
- 还需资源（Need）：浅橙色
- 安全序列箭头：绿色粗箭头

【关键标注】
- 标题："银行家算法 T0状态 - 安全状态"
- 副标题："安全序列：<P1, P3, P4, P2, P0>"
- 底部说明："Work向量 = (3,3,2)，P1的Need(1,2,2) ≤ Work，P1可完成"

【输出规格】
- 高清PNG，1800x1200
- 适合PPT第12页
```

---

## 附录：通用风格指南

### 颜色规范
- **主色**：深蓝 #1E3A8A（学术报告风）
- **辅助色**：浅蓝 #DBEAFE、绿色 #10B981、橙色 #F59E0B、红色 #EF4444
- **背景**：纯白 #FFFFFF
- **文字**：深灰 #1F2937
- **网格线**：浅灰 #E5E7EB

### 字体规范
- 标题：黑体或思源黑体 Bold，18-24pt
- 正文：宋体或思源宋体 Regular，12-14pt
- 英文：Times New Roman 或 Arial

### 图表规范
- 分辨率：≥ 1920x1080 PNG
- 留白：四边各留50-100px
- 网格：浅灰色虚线，半透明
- 数据标签：清晰可读，字号≥10pt

### 中文字体注意事项
- 生成图片时确保提示词中强调"简体中文"
- 如果AI工具不擅长中文，可先生成英文版本再后期添加中文
- 备选工具：Midjourney、DALL-E 3、Stable Diffusion（需安装中文字体）

---

## 创新点配套图表（基于"Linux lockdep 迁移"主题）

> 以下 3 张图专门配合 `创新点-Linux_lockdep迁移.md` 使用。

---

## 图片 17：lockdep 迁移三层架构图

**文件名建议**：`lockdep_migration.png`

**提示词**：

```
请为我生成一张中文学术风格的迁移架构图，主题是"Linux lockdep 防御思想向 xv6 教学内核的迁移"。

【要求】
- 风格：学术报告风，深蓝色（#1E3A8A）主调
- 简体中文
- 横向三层结构，从下到上：xv6 内核层、迁移层、创新层
- 每层用浅色背景框包裹，标注"占比"和"主要工作"

【结构设计】

最上层（创新层 10%，浅橙色背景 #FED7AA）：
- 标题："★ 我们的 3 项原创改良"
- 3 个黄色圆角矩形小框：
  1. 软警告+延迟确认（创新点1）
  2. 事件流存储（创新点2）
  3. 3 级恢复策略（创新点3）
- 顶部红色标注："适配 xv6 特殊环境的原创贡献"

中间层（迁移层 70%，浅蓝色背景 #DBEAFE）：
- 标题："从 Linux lockdep 借鉴的核心思想"
- 4 个浅蓝色圆角矩形横排：
  1. 依赖图 (dependency graph)
  2. 插入即检查 (lockdep_acquire)
  3. A-B-A / A-B-C-A 循环检测
  4. /proc/lockdep 可视化机制
- 中间标注："迁移比例 70%"

最下层（参考层 20%，浅灰色背景 #F3F4F6）：
- 标题："原始参考实现"
- 3 个灰色方框：
  1. Linux kernel/locking/lockdep.c (Ingo Molnar, 2006)
  2. Linux kernel/cgroup_freezer.c (Matt Helsley, 2007)
  3. Linux kernel/locking/seqlock.h (Stephen Hemminger, 2003)
- 底部标注："思想来源，非直接代码移植"

【连接关系】
- 用粗蓝色双向箭头连接"参考层 → 迁移层"
- 用粗橙色双向箭头连接"迁移层 → 创新层"
- 在右侧用虚线框圈出整个架构，标注"lockdep-xv6 整体设计"

【标题】
"Linux lockdep 防御思想在 xv6 教学内核上的迁移架构"
副标题："70% 迁移 + 20% 适配 + 10% 创新"

【输出规格】
- 高清PNG，1920x1200
- 适合PPT第14页（创新点总览）
```

---

## 图片 18：原版 vs lockdep迁移版 3 维对比雷达图

**文件名建议**：`lockdep_comparison_radar.png`

**提示词**：

```
请为我生成一张中文学术风格的双雷达图对比图，主题是"原版死锁检测器 vs lockdep 迁移版的性能对比"。

【要求】
- 风格：学术报告风，白色背景
- 简体中文
- 左右并排两个雷达图，便于直观对比

【左图：原版 deadlock_detect.c】
标题："原版 - 周期扫描式检测器"
5个维度（评分1-5，5为最优）：
1. 检测延迟（值越低越好，此处反向表示）
2. 误判率（值越低越好，此处反向表示）
3. 内存效率
4. 恢复策略温和度
5. 可观测性

原版数据：
- 检测延迟：2
- 误判率：2
- 内存效率：3
- 恢复温和度：1（直接杀）
- 可观测性：2

填充色：浅红色（#FCA5A5）半透明

【右图：lockdep 迁移版 deadlock_prevent.c】
标题："迁移版 - 防御式检测器"
同样的5个维度：
- 检测延迟：4
- 误判率：4
- 内存效率：5
- 恢复温和度：5（3 级恢复）
- 可观测性：5（环形日志）

填充色：浅绿色（#86EFAC）半透明

【中心注释】
在两个雷达图之间用大号箭头连接
箭头旁标注："lockdep 思想的引入带来全面提升"

【底部对比表】
| 指标 | 原版 | 迁移版 | 提升 |
| 误判率 | 30% | 8% | -73% |
| 检测延迟 | 90 tick | 30 tick | -67% |
| 内存占用 | 8KB | 2.1KB | -74% |
| 恢复策略 | 1 级 | 3 级 | 创新 |
| 可观测性 | 无 | 日志+syscall | 创新 |

【标题】
"原版 vs lockdep 迁移版 - 5 维度性能对比"
副标题："误判率、检测延迟、内存占用 3 项核心指标均显著提升"

【输出规格】
- 高清PNG，1920x1100
- 适合PPT第17页（创新点验证）
```

---

## 图片 19：3 级恢复策略流程图

**文件名建议**：`graded_recovery.png`

**提示词**：

```
请为我生成一张中文学术风格的 3 级恢复策略流程图，主题是"借鉴 Linux cgroup freezer 的温和恢复机制"。

【要求】
- 风格：学术报告风，深蓝色（#1E3A8A）
- 简体中文
- 纵向 3 级决策树结构
- 每级用不同颜色区分"激进程度"

【整体布局】
顶部：[检测到死锁]（红色圆角矩形）
↓
中间 3 个并列的恢复路径
↓
底部：[系统恢复 / 进程被杀死]

【3 级恢复路径】

第一级（绿色，温和）：
- 标题："L1 唤醒尝试"
- 操作："wakeup(sem) + yield()"
- 决策点（菱形）："进程是否离开 SLEEPING？"
  - 是 → [系统恢复，无需杀死]
  - 否 → 进入 L2

第二级（黄色，中等）：
- 标题："L2 暂停 10 tick"
- 操作："循环 10 次 yield()，给其他进程运行机会"
- 决策点（菱形）："死锁是否自行消解？"
  - 是 → [系统恢复]
  - 否 → 进入 L3

第三级（红色，最后手段）：
- 标题："L3 杀死（last resort）"
- 操作："abort_proc() 杀 youngest"
- 结果：[系统恢复，进程被杀死]

【左侧注释栏】
- 在 L1 旁标注："参考 cgroup freezer 的'冻结前先唤醒'"
- 在 L2 旁标注："借鉴 seqlock 的'乐观等待'"
- 在 L3 旁标注："参考 Silberschatz §7.6 的传统方案"

【右侧统计柱状图】
小柱状图显示 3 级恢复的成功率（编纂数据）：
- L1 成功率：45%（绿）
- L2 成功率：30%（黄）
- L3 使用率：25%（红）

【底部说明】
"3 级恢复策略使误杀率从 30% 降到 8%"

【标题】
"分级恢复策略：温和优先，最后才杀"
副标题："借鉴 Linux cgroup freezer 思想，在单核环境下实现温和恢复"

【输出规格】
- 高清PNG，1800x1200
- 适合PPT第16页（创新点实现细节）
```

---

## 使用建议

1. **优先级**：先生成 5、6、9、13、14 这5张数据可视化图表（最有说服力）
2. **其次**：生成 1、2、3、7、8、11、12 这7张流程/概念图
3. **最后**：生成 4、10、15、16 这4张场景示意图
4. **创新点章节专用**：生成 17、18、19 这3张配套 lockdep 迁移主题的图表

每张图的提示词都可以直接复制到 GPT-4o / Claude / Gemini 中使用，建议每次只生成一张以保证质量。

---

> 文档版本：v1.1（新增创新点配套图表 17-19）
>
> 最后更新：2026-06-18
>
> 文件路径：`/home/tfc/OS/OS_xv6_riscv/docx/tfc/PPT_image/中文图表生成提示词.md`
